import React from "react";
import { LoginForm, SessionContainer } from "../";

const LoginPage = () => {
  return (
    <SessionContainer>
      <LoginForm />
    </SessionContainer>
  );
};

export { LoginPage };
